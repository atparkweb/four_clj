(ns four-clj.p83-test
  (:require [clojure.test :refer :all]
            [four-clj.p83 :refer :all]))


(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))

(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))

(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))

(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))

(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))

(deftest half-truth-1
  (testing "half-truth-1"
    (is (= false (half-truth false false)))))
