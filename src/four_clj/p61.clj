(ns four-clj.p61)

; Write a function which takes a vector of keys and a vector of values and constructs a map from them.
; Special Restrictions: zipmap

(defn map-cons [key-coll v-coll]
  (throw (.Exception "Not implemented")))
